---
phase: 08-rank-showcase
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/rank-showcase.spec.ts
  - tests/utils/setup.ts
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
  - RANK-05
  - RANK-06
  - RANK-07

must_haves:
  truths:
    - "Test file exists with 7 test stubs covering all RANK requirements"
    - "Helper functions exist for rank showcase test assertions"
    - "All test stubs are marked as pending (test.skip or test.fixme) so they pass in CI"
  artifacts:
    - path: "tests/e2e/rank-showcase.spec.ts"
      provides: "E2E test stubs for rank showcase feature"
      min_lines: 50
    - path: "tests/utils/setup.ts"
      provides: "Updated test helpers with rank showcase utilities"
      contains: "getRankCards"
  key_links:
    - from: "tests/e2e/rank-showcase.spec.ts"
      to: "tests/utils/mockAuth.ts"
      via: "import signInTestUser for authenticated tests"
      pattern: "signInTestUser"
---

<objective>
Create test infrastructure (Wave 0) for the Rank Showcase feature before implementation begins.

Purpose: Establish test stubs and helper functions so Wave 1 implementation tasks have automated verification targets. Satisfies Nyquist compliance requirement from VALIDATION.md.

Output: Test file with 7 pending E2E test stubs + 3 helper functions in setup.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rank-showcase/08-CONTEXT.md
@.planning/phases/08-rank-showcase/08-VALIDATION.md
@tests/e2e/achievements-authenticated.spec.ts
@tests/utils/setup.ts
@tests/utils/mockAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rank showcase E2E test stubs and helper functions</name>
  <files>tests/e2e/rank-showcase.spec.ts, tests/utils/setup.ts</files>
  <action>
**1. Add helper functions to `tests/utils/setup.ts`:**

Append three new exported functions at the end of the file (do NOT modify existing functions):

```typescript
/**
 * Get all rank cards on the Achievements page.
 * Each rank card is an .achievement-card inside the RANK PROGRESSION panel.
 */
export async function getRankCards(page: Page) {
  return page.locator('[data-testid="rank-card"]');
}

/**
 * Get the current rank card (the one with gold glow).
 */
export async function getCurrentRankCard(page: Page) {
  return page.locator('[data-testid="rank-card"][data-current="true"]');
}

/**
 * Get the guest sign-in message shown when not authenticated.
 */
export async function getGuestRankMessage(page: Page) {
  return page.locator('text=SIGN IN TO UNLOCK RANK PROGRESSION');
}
```

**2. Create `tests/e2e/rank-showcase.spec.ts`:**

Follow the pattern from `achievements-authenticated.spec.ts`:
- Import `test, expect` from `@playwright/test`
- Import `signInTestUser` from `../utils/mockAuth`
- Import `getRankCards, getCurrentRankCard, getGuestRankMessage` from `../utils/setup`
- Skip all tests if `process.env.EMULATORS_AVAILABLE !== 'true'` (same pattern as achievements-authenticated)
- `beforeEach`: sign in test user, navigate to `/achievements`, wait for page load

Create 7 test stubs using `test.fixme()` (marks them as known-failing/pending, passes in CI):

```
test.fixme('should display all 15 ranks', ...)
  // RANK-01: getRankCards count === 15

test.fixme('should show rank details for each card', ...)
  // RANK-02: Each card has rank number, name, tagline, XP threshold text

test.fixme('should highlight current rank with gold glow', ...)
  // RANK-03: getCurrentRankCard is visible, has god-mode-glow class

test.fixme('should show earned and locked rank states', ...)
  // RANK-04: Cards before current have full opacity, cards after current have .locked class

test.fixme('should display rank showcase above achievements section', ...)
  // RANK-05: RANK PROGRESSION panel appears before ACHIEVEMENTS heading in DOM order

test.fixme('should show progress indicator on current rank', ...)
  // RANK-06: Current rank card contains "+XXX XP to" text or "MAX RANK ACHIEVED"

test.fixme('should show sign-in message for guest users', ...)
  // RANK-07: Guest test (no signInTestUser in beforeEach for this one)
  // Navigate to /achievements without auth, getGuestRankMessage is visible
```

For the guest user test (RANK-07), do NOT use the shared beforeEach with signInTestUser. Instead, use a separate describe block or handle auth skip within the test itself. Pattern:

```typescript
test.describe('Rank Showcase - Guest', () => {
  // No skip for emulators - guest tests work without emulators
  test.fixme('should show sign-in message for guest users', async ({ page }) => {
    await page.goto('/achievements');
    // Guest users get redirected to login or see guest message
    const guestMsg = await getGuestRankMessage(page);
    await expect(guestMsg).toBeVisible();
  });
});
```

**Important:** Use `test.fixme()` not `test.skip()` -- fixme signals "known to be unimplemented" while skip signals "intentionally excluded". Both pass in CI but fixme is semantically correct here.

**Data-testid contract for RankShowcase component (Plan 08-01 will implement these):**
- Each rank card element: `data-testid="rank-card"`
- Current rank card additionally: `data-current="true"`
- This ensures test selectors work when implementation lands.
  </action>
  <verify>
    <automated>cd /Users/radekmuzikant/Documents/doom-tracker && npx playwright test tests/e2e/rank-showcase.spec.ts --list 2>&1 | head -20</automated>
  </verify>
  <done>rank-showcase.spec.ts exists with 7 test stubs that are listed by Playwright. setup.ts has 3 new helper functions (getRankCards, getCurrentRankCard, getGuestRankMessage). All stubs use test.fixme() so they pass in CI without implementation.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/e2e/rank-showcase.spec.ts --list` shows 7 tests
2. `npx tsc --noEmit` passes (TypeScript compiles)
3. `npm run test:e2e` passes (fixme tests are skipped gracefully)
</verification>

<success_criteria>
- tests/e2e/rank-showcase.spec.ts exists with 7 test stubs (one per RANK requirement)
- tests/utils/setup.ts has getRankCards, getCurrentRankCard, getGuestRankMessage helpers
- All tests listed by Playwright and pass in CI (fixme = skipped)
- Data-testid contract documented for Plan 08-01 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/08-rank-showcase/08-00-SUMMARY.md`
</output>
